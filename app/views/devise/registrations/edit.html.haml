.jumbotron
  %h2
    Edit #{resource_name.to_s.humanize}
  = bootstrap_form_for(resource, url: registration_path(resource_name),html: { method: :put }, layout: :horizontal, label_col: "col-sm-4 ", control_col: "col-sm-4") do |f|
    = devise_error_messages!
    = f.text_field :name, label:"Company name"
    = f.email_field :email
    = f.password_field :password, label: "New password*"
    = f.password_field :password_confirmation, label: "Confirm new password*"
    = f.password_field :current_password
    * - enter only if you want to change your password
    = f.form_group do
      = f.submit "Update", class: "btn-primary"

